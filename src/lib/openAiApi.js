//en este va get api 
/*import { getApiKey } from './apiKey.js';

const messages =[]; //arreglo vacio para guardar el historial

export const communicateWithOpenAI = (messages) => { //se importa en el chat individual
//Aquí es donde debes implementar la petición con fetch pide datos en js
  const apikey =  getApiKey ();  //para llamar a la apikey;
  const openAi = "https://api.openai.com/v1/audio/translations";  //define la url de openai
 

/ model =  "gpt-4o-mini"; //define eñ modelo del chat

  const openai = new OpenAI();

  async function main() {
    const completion = await openai.chat.completions.create({
      messages: [{ role: "system", content: "Hola soy You are a helpful assistant." }],
      model: "gpt-4o",
    });
  
    console.log(completion.choices[0]);
  }
  
  main();
};*/
